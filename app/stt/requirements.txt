fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
soundfile>=0.12
numpy>=1.26
librosa>=0.10        # optional, used for resampling if mic capture != 16k
# Apple Silicon (default):
parakeet-mlx>=0.3 ; sys_platform == "darwin"
# Optional CUDA backend; uncomment if you want NeMo fallback:
# nemo_toolkit[asr]>=2.0
