{
  "name": "bridge-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge 3-day prototype — local orchestrator. Receives intent text, calls OpenRouter, returns a tile spec. Executes selected actions on local backends.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test --test-reporter=spec"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
